# Vendor settings used by the EDGELIB Packer
# This file contains a maximum of 16 vendors which are defined by a 4 character string
# and a pack unlock code

# Important:
# - The first vendor ID is used as default
# - It reads vendor.ini files in the following order: first it reads all vendor ID's from
# - vendor.ini from the current folder, then it reads vendor.ini from the EDGELIB Packer folder.

FREE = 0x0
